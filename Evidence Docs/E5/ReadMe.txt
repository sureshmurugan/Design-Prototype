1.As per design Style Guide Sub Headings use only <H4> tag. The
same format used in Subheadings in the project.
2.As per design Style Guide INLINE CSS properties should be wrapped in code. 
Inline property assignments should not include the semicolon.same format used in Inline Css in the project.
3.As per design Style Guide External CSS blocks should be written with each property on a different line and with each selector on a different line.
same format used in External Css blocks in the project.